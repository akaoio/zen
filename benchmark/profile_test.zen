# Small profiling test to identify bottlenecks
function fib n
    if n <= 1
        return n
    return fib n - 1 + fib n - 2

# Calculate fibonacci(20) to get measurable but not excessive runtime
set result fib 20
print "Fibonacci(20) = " + toString result