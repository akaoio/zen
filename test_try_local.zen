class TestCalc
    function divide x y
        if y = 0
            throw "Division by zero"
        return x / y

set c new TestCalc

try
    c.divide 10 0
catch error
    print "Caught error:" error