# Memory stress test - create many instances
class MemTest
    method data
        return "x" * 100

print "Starting memory test"

# Create 100 instances
for i in 1..100
    set var new MemTest
    if i % 10 = 0
        print "Created " + i + " instances"

print "Memory test complete"