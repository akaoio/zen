import e from"crypto";const t=864e5;export function inspect(n,a=Date.now()){let r;try{r=new e.X509Certificate(n)}catch{return{state:"unreadable"}}const i=Date.parse(r.validFrom),s=Date.parse(r.validTo);if(!Number.isFinite(i)||!Number.isFinite(s))return{state:"unreadable"};const o=Math.floor((s-a)/t);let c;return c=a>=s?"expired":a<i?"not-yet-valid":s-a<=14*t?"expiring":"ok",{subject:r.subject,validFrom:i,validTo:s,daysLeft:o,state:c}}function n(e){return 1===Math.abs(e)?"":"s"}function a(e){return new Date(e).toISOString().replace(/\.\d+Z$/,"Z")}export function describe(e,t){switch(t.state){case"expired":{const r=Math.abs(t.daysLeft);return`${e} EXPIRED ${r} day${n(r)} ago (${a(t.validTo)}) — every TLS handshake is being refused`}case"expiring":{const r=t.daysLeft;return`${e} expires in ${r} day${n(r)} (${a(t.validTo)}) — renewal has not happened`}case"not-yet-valid":return`${e} is not valid until ${a(t.validFrom)} — check this machine's clock`;case"unreadable":return`${e} could not be parsed as a certificate — serving TLS with something unreadable`;default:return null}}export function check(e,t=Date.now()){const n=[];for(const{label:a,pem:r}of e){if(!r)continue;const e=describe(a,inspect(r,t));e&&n.push(e)}return n}