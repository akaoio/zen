/**
 * lib/axe.js — BACKWARD COMPATIBILITY SHIM
 *
 * AXE has been moved into src/axe.js and is now built into zen.js / zen.min.js.
 * Importing zen.js automatically activates AXE — no separate import needed.
 *
 * This file is kept so that existing code that explicitly imports lib/axe.js
 * continues to work without errors. It is a no-op: AXE is already active.
 *
 * @deprecated — remove this import; AXE is in the zen.js core bundle.
 */
