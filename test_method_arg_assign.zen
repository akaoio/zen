class Test
    function greet name
        return "Hello, " + name

set t new Test

# Standalone method call (should work)
t.greet "World"

# Method call in assignment (might not work)  
set msg t.greet "World"
print msg