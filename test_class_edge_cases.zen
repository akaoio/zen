# Edge Case 1: Empty class
class Empty

print "1. Empty class defined"

# Edge Case 2: Class with no methods but trying to create instance
set e new Empty
print "2. Empty instance created"

# Edge Case 3: Multiple classes with same method names
class Cat
    method speak
        return "Meow"

class Dog
    method speak
        return "Woof"

set cat new Cat
set dog new Dog
print "3. Multiple classes created"

# Edge Case 4: Class name conflicts with variables
set Animal 42
class Animal
    method test
        return "I'm a class"

print "4. Class overwrites variable"

# Edge Case 5: Nested class definitions (should fail)
class Outer
    class Inner
        method test
            return "nested"

print "5. Should not reach here - nested classes"