# Test advanced ZEN syntax features
set numbers 1, 2, 3, 4, 5

# Test ternary expressions
set max_val numbers.length > 3 ? 10 : 5
print max_val

# Test null coalescing
set result null ?? "default value" 
print result

# Test compound assignment
set counter 0
counter += 5
counter *= 2
print counter

# Test range expressions
for i in 1..10
    print i

# Test array destructuring
set arr 10, 20, 30
set first, second = arr
print first
print second

# Test lambda expressions
set doubler x => x * 2
set doubled doubler 5
print doubled

# Test optional chaining (if supported)
set obj name: "test", details: null
set value obj.details?.length ?? 0
print value