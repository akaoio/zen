function fibonacci n
    if n <= 1
        return n
    return fibonacci n - 1 + fibonacci n - 2

set numbers 1, 2, 3, 4, 5
set results

for num in numbers
    set fib_result fibonacci num
    print "Fibonacci of " + toString num + " is " + toString fib_result

print "All done\!"
