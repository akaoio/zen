print "=== ADVANCED VARIABLE TESTS ==="
set x 10
set y 20
set sum x + y
print "x = " + x + ", y = " + y + ", sum = " + sum

set message "Hello"
set target "ZEN"
set greeting message + " " + target
print "Greeting: " + greeting

set factor 3
set base 7
set product factor * base
print "Product: " + factor + " * " + base + " = " + product

print "Variables work correctly!"